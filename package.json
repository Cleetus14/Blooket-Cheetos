{
  "name": "blooket-cheetos",
  "version": "1.6.7",
  "private": true,
  "description": "Bookmarklet-based Blooket client cheat.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node scripts/build.mjs",
    "test": "npm run smoke && npm run test:runtime",
    "smoke": "node scripts/smoke.mjs",
    "test:runtime": "node scripts/runtime.mjs",
    "dev": "node scripts/build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.6.3"
  }
}